"""
Wave Glider platform package.

New Wave Glider–only business logic belongs here. Existing WG code remains under
``app.core`` (data/sync/stations/reporting) until a focused peel is justified.

Register the platform in ``app.core.platforms.registry`` before adding modules.
HTTP routers stay under ``app.routers``.
"""
