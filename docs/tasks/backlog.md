# Backlog

Roughly ordered by priority, top = next up. Move an item to `in-progress.md`
when you start it; move it to `done.md` when it ships.

Keep each line self-contained enough that the AI assistant can pick it up cold:
what, where, and any constraint that matters.

Seeded 2026-07-29 from recent unfinished-work review
([Review of unfinished tasks](81c85f36-51b6-4c5b-9258-b0923532df06)) plus a light archive skim.

## High priority

- [ ] Finish CLS Argos live verify (blocked on admin confirming new M2M login credentials): put `ARGOS_USERNAME` / `ARGOS_PASSWORD` in `.env`; run `python scripts/test_argos_access.py --device-ref <CLS_deviceRef>` per glider; confirm admin Mission Overview `argos_id` matches CLS `deviceRef`; open a realtime daily checklist and confirm `argos_gps_check_val` + pre-suggested `argos_monitor_val`. Code already in-repo (`argos_client`, `argos_cache_service`, checklist autofill) — [Slocum how-to Argos](../wiki/how-tos/slocum.md#daily-pilot-checklist)
- [ ] Confirm prod `data/` and `data_store/` ownership (`chown cove:cove` or service user) and reclaim stranded `*.tmp` if mission CSV sync still fails — see AGENTS.md NFS/`os.replace` notes; chat [Mission CSV rename fix](c7ca764b-4f6c-42b2-a304-54183c563871)
- [ ] Commit, QA, and deploy the outstanding theme + chart hover/crosshair UI batch (keep as its own commit — not mixed with docs) — [Theme consistency](80b24643-2d50-47fd-96bb-2731d082fd3b), [Chart hover fix](c7ac3551-0b33-4987-a8c6-4d2e34e41304)
- [ ] Prod verify: auto-checklist actually submitting, Iridium map toggle on prod, UTC hardening / sensor cards as applicable; DMON card on a DMON-equipped glider (byte-count chart + ASC 48h list + green/red gap status dot + gated checklist Science items after schema **12** mirror rebuild + SFMC refresh). Robots4Whales review UI/report path is implemented in-repo — confirm on prod after deploy (URL set, 12h cache job, weekly PDF section).
- [ ] Mission catalog production replay (in progress): commit catalog-only (not mixed with theme/UI); deploy flags **false**; do not copy laptop SQLite; dry-run → inspect Fundy duplicate → `--apply` → WG/AUTO_APPLY/Slocum flags in order — [mission_catalog_cutover.md](../wiki/how-tos/mission_catalog_cutover.md) / [in-progress](./in-progress.md)

## Medium priority

- [ ] Map vector layers — remaining follow-ups (home overlays + catalog toggles + DFO FMA set shipped; how-to [map_vector_layers.md](../wiki/how-tos/map_vector_layers.md)): (0) commit/deploy feature + enable `map_vector_layers` on staging/prod when ready; (1) optional manifest `default_on` and/or per-mission/deployment layer id list (“always show”); (2) generalize `scripts/convert_map_layer_kml.py` beyond DSZ/safe-zone name splits; (3) wire selected layers onto public login map (`public_visible` + unauth routes); (4) further simplify large `dfo_fma_*` GeoJSON (~2–4 MB each) and/or re-fetch northern shrimp `OBJECTID` 19 when egisp stops 500ing — idea inbox 2026-08-08
- [ ] In-app map layer uploader (admin): accept defined document types (e.g. KML/KMZ/GeoJSON — whitelist explicitly) and/or ArcGIS/ESRI REST links; ingest into `config/map_layers/` catalog (reuse `convert_map_layer_kml.py` / `fetch_map_layer_arcgis.py` logic); today is offline script + commit — [map_vector_layers.md](../wiki/how-tos/map_vector_layers.md) — idea inbox 2026-08-09
- [ ] AIS vessel density follow-ups (monthly All-types v1 shipped; how-to [vessel_density_map_layer.md](../wiki/how-tos/vessel_density_map_layer.md)): (0) enable `vessel_density_map_layer` on staging/prod when ready; (1) yearly vessel-class subtoggles (MapServer layers 1–6: All / Cargo / Fishing / Other / Passenger / Tanker); (2) optional opacity slider; (3) public login map wiring; (4) optional cache cleanup/quota job for `data_store/vessel_density_cache/` — idea inbox 2026-08-09
- [ ] CIOPS-East ice forecast follow-ups (v1 shipped 2026-08-24; how-to [ciops_ice_map_layer.md](../wiki/how-tos/ciops_ice_map_layer.md)): (0) enable `ciops_ice_map_layer` on staging/prod when ready; (1) **winter visual soak** — confirm coloured ice tiles over Newfoundland / Labrador / GSL (August often near-zero / blank with 200s is expected); optional UX: fit-to-domain or “outside CIOPS-East” status so empty ≠ broken; (2) CIOPS-West; (3) GetFeatureInfo click; (4) style picker / opacity / animate; (5) public login map; (6) optional cache cleanup for `data_store/ciops_ice_cache/` — idea inbox 2026-08-24
- [ ] Bathymetry layer on all platform maps (home WG/Slocum, public login map, mission maps as applicable): reuse reporting ETOPO stack (`app/core/geo/bathymetry.py`, `data_store/bathy_cache/`, `report_bathymetry_contours` patterns) as a Leaflet toggleable overlay — contours and/or shaded depth; share cache/cleanup jobs — idea inbox 2026-08-08
- [ ] DFO fishery notices & openings/closings tracker: daily (or similar) check of DFO fishery notices / opening–closing postings; maintain a running status of known fisheries (open/closed/seasonal notes) for ops awareness; leader job + admin/pilot UI. **Map integration (future):** join notice/opening status to existing home-map FMA / LFA polygons (`dfo_fma_*`, `dfo_lobster_lfa_2022` via `map_vector_layers` / `vector_map_layer.js`) — e.g. style by open/closed, popup summary, optional filter “show only open areas”; keep static GeoJSON snapshots separate from live notice feed — idea inbox 2026-08-09
- [ ] Banner platform switcher as dropdown (WG ↔ Slocum) instead of only linking to `/platform`; keep the platform splash page for initial login / first choice — `_banner.html` — idea inbox 2026-08-09
- [ ] Mission media “Default Media” flag: mark one image/video per mission as default for weekly/end-of-mission report cover or inset and public login map display; admin/pilot control in existing mission media UI (`mission_media`); only one default at a time — idea inbox 2026-08-09
- [ ] Admin runtime mission & config control (avoid `.env` edit + restart where possible): add/list missions without touching `ACTIVE_SLOCUM_DATASETS` / `ACTIVE_REALTIME_MISSIONS` / `HISTORICAL_*`; move active → historical (and for Slocum, flip ERDDAP pointer realtime ↔ delayed); broader admin UI to edit feature toggles (and similar non-secret ops flags — prefer `FEATURE_TOGGLES_FILE` / DB overlay + invalidate `feature_toggles` cache). Keep secrets in `.env`; document what still requires restart — idea inbox 2026-08-05
  - **Partial (2026-08-15):** live-key-safe catalog (ADR 0005) + local consumer flags soaked (WG sync, AUTO_APPLY, Slocum warm; env-equivalent keys; dashboards 1:1 with prod). **Now:** production replay (see high priority / in-progress). **After prod soak:** admin unmatched-source UI; public map last; retire `ACTIVE_*` / folder-map inventory lists once set-equality holds. Do not sync the ~225 ST extras or auto-CONTINUOUS. Ops: [mission catalog cutover](../wiki/how-tos/mission_catalog_cutover.md)
- [ ] Catalog unmatched ERDDAP sources admin UI: show/review the leftover unmatched datasets (currently `bond_test_for_sci_water_depth_realtime`, `unit_1190_20250515_214_delayed`) without auto-creating missions — after prod catalog apply — idea inbox 2026-08-15
- [ ] Public login map membership from catalog (last consumer): keep `public_map_enabled` ∩ env allowlists until prod catalog flags soak; then route through enablement the same way as WG sync / Slocum warm (still exact env strings first) — `app/core/public_map_service.py` — idea inbox 2026-08-15
- [ ] Retire hardcoded `ACTIVE_REALTIME_MISSIONS` / `ACTIVE_SLOCUM_DATASETS` / historical lists as membership authority only after prod set-equality soak; keep `.env` as override/fail-safe until then — ADR 0005 — idea inbox 2026-08-15
- [ ] Optional: merge split historical Wave Glider overview PKs (`m211` vs `1070-m211`, similar `####-m###` leftovers) so children (notes/media/EOM) live on one `m###-SV3-####` row; do **not** blindly delete either side — idea inbox 2026-08-15
- [ ] Optional: CLS Argos leader prefetch job (warm `data_store/argos_cache/` every 6–12h for deployments with `argos_id`) — v1 uses on-demand checklist fetch + 30 min TTL
- [ ] Slocum chart depth overlay (`m_depth` / Show depth) must not follow Resample/`granularity_minutes` of the primary series — keep full-resolution (or independently stride-limited) depth background while other variables may be granulated; today bulk chart path resamples `m_depth` with the same interval (`slocum_dashboard.js` + `chart-data-bulk`) — idea inbox 2026-08-09
- [ ] SFMC surface dialog / surface-log fallback when file transfers lag: inventory what we can already pull (`fetch_surface_events_payload`, `fetch_dialog_checklist_values` / `parse_surface_dialog_log`, glider log tail APIs in `sfmc_client.py` — today used mainly for checklist autofill); plan a dashboard (or Vehicle Health) showcase of key surface-dialog values for long no-telemetry / slow from-glider periods — idea inbox 2026-08-07
- [ ] Slocum weekly telemetry speed scale revisit: keep SFMC-style surfacing SOG coloring (`color_by=sfmc_sog`, hard cap 1.1 kt for now); consider clamped dynamic vmax (e.g. nice P95 between ~0.6–1.1) so quiet weeks use more of the palette — chat [SFMC speed track](592cc7c9-a674-4a8c-a04f-15683bac4a10)
- [ ] Slocum weekly report polish Phase 2+: landscape multi-panel sensors, checklist-bundle charts, end-of-mission mode — content polish (summary KPIs, SOG track, CTD profiles, DMON whale title) shipped 2026-08-04; `weekly_report_url` persistence shipped with public map 2026-08-05; remaining items still open — [Report polish Phase 1](b8a79301-0344-450e-99c7-0d8d67d64032)
- [ ] Modular report requests: generate PDFs (or report sections) for subsets — dataset slices, telemetry plot groups, and individual sensors — rather than only the full weekly bundle; API/UI to pick modules + date window; reuse `app/core/reporting` section builders where possible — idea inbox 2026-08-04
- [ ] Refine report expanded notes (ephemeral v1 shipped 2026-08-07): hone UX copy/placement of the include toggle + textarea (home modal, admin WG/Slocum overviews), PDF “Additional notes” layout/heading after Publication table, validation/length limits, and whether to persist notes later; keep automated weekly reports free of user narrative — idea inbox 2026-08-07
- [ ] Mission pre-launch / pre-deployment checklist inclusion: wire existing `pre_deployment_checklist` form (`app/forms/form_definitions.py`) into mission/deployment workflow and weekly + end-of-mission reports. Later (Sensor Tracker dependency, partly external): automate assignment of data loggers, instruments, and sensors onto a platform deployment from Tracker metadata — idea inbox 2026-08-04
- [ ] Sensor Tracker–driven deployment auto-config: on mission sync, auto-toggle platform `enabled_sensor_cards` from Tracker instruments/sensors. For Slocum, also map Tracker ballast pump type → `glider_depth_class` (deep/shallow) and power id → `battery_pack` into `checklist_reference_values` presets (`checklist_autofill.py`) — idea inbox 2026-08-05. Precursor: Team Sensor Tracker browser (`/team/sensor-tracker`)
- [ ] Sensor lifetime / calibration tracker (pairs with expanded Sensor Tracker integration): tally days at sea, days on the shelf, days since last calibration, and expected lifetime / calibration intervals for platform instruments, components, and sensors; surface warnings when due — may need Tracker fields or a local overlay if Tracker lacks them — idea inbox 2026-08-08. **Partial (2026-08-20):** Team Sensor Tracker detail shows live days at sea (all missions) / current deployment / attached / shelf (`sensor_tracker_analytics.py`; no new tables). Instruments include logger-mounted hops; sensors follow `current_instrument` when `sensor_on_instrument` is empty; components use `component_on_platform` and related Platforms / overlapping Deployments. Still open: calibration age, lifetime expectations, dedicated Team hub shelf UI. Precursor browse: `/team/sensor-tracker`; do not duplicate that live query UI
### Team hub (platform-agnostic)

Authenticated in-app section (own nav entry + landing page), distinct from WG/Slocum mission dashboards. Shell + first live script are in `in-progress.md`; remaining items hang off that shell. Split from single idea-inbox line 2026-08-10.

- [ ] One-off plotting / analysis space (inside Team hub): durable, shareable home for ad hoc exploratory plots/analyses that don’t belong on a mission dashboard (today scattered in `exploration/` or local machines); decide storage/retention when scoped. Distinct from the named **Visualizations gallery** (`/team/visualizations` — Sensor Tracker fleet charts) — idea inbox 2026-08-11
- [ ] Team (non-mission) checklists (inside Team hub): general ops/onboarding/pre-season checklists, distinct from mission daily checklists (`pic_handoff_checklist`, `pre_deployment_checklist`, Slocum daily checklist); reuse `app/forms/form_definitions.py` patterns where sensible — idea inbox 2026-08-11
- [ ] Component time-at-sea / shelf UI (inside Team hub): UI surface over the Sensor lifetime / calibration tracker and Sensor Tracker–driven deployment auto-config backlog items above — do **not** duplicate tracker logic; hub consumes `sensor_tracker_analytics.py` (already used on `/team/sensor-tracker` detail). Live inventory browse is the Team Sensor Tracker browser — idea inbox 2026-08-11

- [ ] Wave Glider daily checklist (system health / mission status): beyond existing pilot forms (`pic_handoff_checklist`, `pre_deployment_checklist`) — a Slocum-style daily deep dive with autofill from telemetry/cache, health & mission-status sections, history/compare where useful; likely under `app/platforms/wave_glider/` — idea inbox 2026-08-06
- [ ] Chart forecast overlay layers: plot Open-Meteo forecasted solar irradiance as a secondary series/layer on the solar power chart (reuse existing `/api/forecast` / Open-Meteo path + weather cache patterns). Generalize where applicable — forecast layers for weather, waves, and ocean surface currents on matching telemetry/dashboard plots; align with declarative chart “forecast” follow-up in chart platform item below — idea inbox 2026-08-05
- [ ] Slocum summary cards mini-trend values: finish/polish sparkline mini-trends on dashboard summary cards (`app/platforms/slocum/summaries.py`, `slocum_dashboard.html` / `slocum_dashboard.js`) — ensure categories that already declare `data-mini-trend` get correct series, extend to cards still missing trends (e.g. dissolved oxygen / sensor cards), and align with declarative chart mini-trend follow-up — idea inbox 2026-08-05
- [ ] Slocum DMON doughnut chart for whale detections over time (species/call-type or period breakdown on the DMON sensor card); reuse Robots4Whales / DMON review cache where useful; fits declarative chart “doughnut” follow-up — idea inbox 2026-08-05
- [ ] Chart platform follow-ups: ERDDAP/narrow bulk API (drop client `rowsToSeries`), server-owned chart config registry, optional spectrum/doughnut/mini-trends/forecast into declarative system, WG↔Slocum schema convergence, JS test harness — [WG declarative rewrite](bcc8cb49-ec97-49b6-b793-8a04926f4140)
- [x] Ops: rename GitHub repo to `Glider-Buddy-System`; remotes updated; backup tag + mirror — [GITHUB_RENAME.md](../wiki/how-tos/GITHUB_RENAME.md) — 2026-08-03
- [ ] Ops (manual on app host): prod path cutover to `/home/cove/Glider-Buddy-System` with symlink soak — no remote access from this workstation; run [`gbs_prod_path_cutover.sh`](../wiki/how-tos/gbs_prod_path_cutover.sh) during a maintenance window — [PROD_PATH_RENAME.md](../wiki/how-tos/PROD_PATH_RENAME.md)

### Platform packages (`app/platforms/`)

This rebrand round is closed for Slocum packaging: core + stragglers live under `app/platforms/slocum/`; CLI is `python -m app.platforms.slocum.cli` (deprecated `app.cli.slocum_cli` shim removed). WG package has first real module `summaries.py` (+ `app/routers/wave_glider.py` soft-refresh API); more WG-only code lands under `app/platforms/wave_glider/`. See [platforms README](../../app/platforms/README.md), ADR 0003, [architecture soft refresh](../wiki/architecture.md#dashboard-summary-soft-refresh).

Notes (not scheduled work):

- **Later:** If the package boundary is paying off, peel one clear WG island (e.g. `app/core/stations/wg_vm4_*`, or WG-only PIC/offload helpers) into `app/platforms/wave_glider/` in a focused PR — not a wholesale core evacuate
- **Later (optional):** thin shared soft-refresh helper / declarative card formatters so WG and Slocum JS don’t fork per sensor — not blocking; pattern is already documented
- **Avoid:** Big-bang “move all Wave Glider out of `app/core`”; keep shared pipelines (`data_service`/loaders/processors/sync), map/weather/bathy/iridium, models, and auth in core. Don’t couple module moves to a full `/api/wave_glider/` URL cutover

## Low priority / someday

- [ ] Wave Glider ERDDAP poke (when realtime WG is on Ocean Track): lift allDatasets maxTime probe into `app/core/data/`; add a WG refresh adapter that writes the existing `data/` / `data_service` path (not `slocum_cache`); one `system_erddap_poke_job` per server; membership = ERDDAP sources only, leave WGMS-only missions on the 60-minute folder sync — [erddap_poke.md](../wiki/how-tos/erddap_poke.md) — idea inbox 2026-08-15
- [ ] Draw static vector map layers (GeoJSON from `config/map_layers/`) on weekly/EOM PDF track maps — reuse published files + manifest styles in `app/core/plotting.py` (`plot_telemetry_page_with_notes` / Cartopy), not Leaflet; optional `include_in_reports` / mission layer list; home overlays already shipped — [map_vector_layers.md](../wiki/how-tos/map_vector_layers.md) — idea inbox 2026-08-08
- [ ] Map vector UI polish: Reference zones toggle group gets crowded with full FMA set — consider species submenu / accordion, search filter, or grouped “Fisheries” section; keep catalog-driven wiring — idea inbox 2026-08-09
- [ ] Interactive `.env` generator script: prompt for required settings, auto-generate `JWT_SECRET_KEY` (and similar hash/secret keys), seed default feature-toggle options (from `config/feature_toggles.example.json`) for optional edits; expandable for more vars. **Security:** keep secrets only in private `.env` — use tempfile / no agent-chat persistence of secret values; never write secrets to docs, logs, or repo — idea inbox 2026-08-06
- [ ] Admin UI to upload/replace brand & platform icons (product SVG/logo, favicon, main-page icons, splash/login art, per-platform SVGs under `web/static/images/` / `platforms/`); today is file-drop per [BRAND_ASSETS.md](../wiki/how-tos/BRAND_ASSETS.md) + `app/core/platforms/registry.py` paths — idea inbox 2026-08-05
- [ ] Admin-defined org theme defaults (seed `ui_preferences` for users without personal overrides) — deferred from Theme Phase B+C; idea inbox 2026-08-07
- [ ] UI density (`comfortable` / `compact`): preference + CSS hooks exist as a subtle placeholder (`.gbs-card` / radius only); define and apply app-wide spacing once the desired look is clear — idea inbox 2026-08-07
- [ ] Optional Robots4Whales-style detection maps in-app (may skip — maps already exist on WHOI); if pursued, present detections differently on home/mission map. Likely needs scraping per-day HTML detection tables beyond the current cached review status ([ADR 0004](../decisions/0004-dmon-robots4whales-review-cache.md)); keep leader cache / no on-request scrape — idea inbox 2026-08-05
- [ ] Update permissions reference matrix (`web/templates/_permissions_modal.html` + `permissions_modal.js`) for new pages/routes and Wave Glider vs Slocum (`can_access_wave_glider` / `can_access_slocum`) divides; keep hand-authored rows aligned with actual router guards — idea inbox 2026-08-05
- [ ] Slocum checklist history follow-ups (multi-day matrix / sparklines); side-by-side compare shipped 2026-07-29 — [Checklist history ideas](378f5e79-578b-4f7a-99cd-ca202e222768)
- [ ] Dockerize app for a more stable host — [Docker migration options](3357a043-5fd2-46e6-bfb5-fd14329e8c7e)
- [ ] Optionally standardize complex error handling in `app/routers/forms.py` and `app/routers/live_kml_router.py` (acceptable as-is; see `docs/archive/reviews/HIGH_PRIORITY_COMPLETION_SUMMARY.md`)
- [ ] Future: if a larger **public** API pool is intentionally built (beyond today’s allowlisted `/api/public/*` map/report surface), consider a curated catalog page in the RU-COOL SLAPI style (type / description / endpoint / format) for external consumers — do **not** dump auth-gated UI/ops routes; keep FastAPI `/docs` for internal use. Prerequisite is a stable public read contract, not documenting the current app API surface — idea inbox 2026-08-12
