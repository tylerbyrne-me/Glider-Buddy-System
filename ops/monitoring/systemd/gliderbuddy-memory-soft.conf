# Phase C (step 1): soft memory pressure on 16 GB host
# Install AFTER staging validation: /etc/systemd/system/gliderbuddy.service.d/memory-soft.conf
# Requires accounting drop-in. Do not combine with memory-hard until soft soak passes.
[Service]
MemoryHigh=10G
