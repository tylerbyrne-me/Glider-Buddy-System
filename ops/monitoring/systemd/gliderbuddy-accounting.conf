# Phase A: accounting only (no hard limits)
# Install: /etc/systemd/system/gliderbuddy.service.d/accounting.conf
[Service]
MemoryAccounting=yes
CPUAccounting=yes
IOAccounting=yes
# OOM visibility in journal when kernel kills the service cgroup
OOMPolicy=continue
