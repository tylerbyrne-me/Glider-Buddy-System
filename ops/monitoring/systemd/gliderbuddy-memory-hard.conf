# Phase C (step 2): hard cap + swap allowance
# Install ONLY after 7-day soft-limit soak with no false alerts
# /etc/systemd/system/gliderbuddy.service.d/memory-hard.conf
[Service]
MemoryMax=12G
MemorySwapMax=2G
